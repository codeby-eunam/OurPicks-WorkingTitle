import 'package:flutter/material.dart';

/// Brand palette ported 1:1 from the RN app (see dangmatch-app inline styles).
class AppColors {
  AppColors._();

  static const primary = Color(0xFFFF6B35); // 주 CTA, 활성 탭
  static const secondary = Color(0xFF1E7874); // 토너먼트/보관함 액션
  static const kakaoYellow = Color(0xFFFEE500);
  static const pwaTeal = Color(0xFF006D77);
  static const pwaBackground = Color(0xFFEDF6F9);

  static const background = Color(0xFFF8F9FA);
  static const surfaceMuted = Color(0xFFF3F4F6);
  static const textPrimary = Color(0xFF111827);
  static const textSecondary = Color(0xFF9CA3AF);
  static const error = Color(0xFFEF4444);
  static const gold = Color(0xFFFFD700);
}
